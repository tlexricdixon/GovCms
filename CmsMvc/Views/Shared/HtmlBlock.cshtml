@using CmsMvc.Services
@inject IHtmlSanitizer HtmlSanitizer
@model Manager.Models.Extend.Blocks.HtmlBlock

@if (!string.IsNullOrWhiteSpace(Model?.Body))
{
    @Html.Raw(HtmlSanitizer.Sanitize(Model.Body))
}